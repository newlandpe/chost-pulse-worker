[build]
  command = "npm run build:netlify"
  publish = "dist"
  functions = "api"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/index"
  status = 200
